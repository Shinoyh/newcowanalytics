services:
  - type: web
    name: newcow-backend
    region: singapore
    env: docker
    plan: free
    envVars:
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      # The database URL is configured in application.yml to use Neon DB
      # If needed, you can override properties via env vars here
